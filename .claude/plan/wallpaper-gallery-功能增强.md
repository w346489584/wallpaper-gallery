# Wallpaper Gallery 功能增强规划

> 生成时间: 2025-12-20
> 规划版本: v1.0

## 一、需求背景

用户希望提升壁纸画廊的用户体验和视觉吸引力，包括：
1. 搜索实时高亮功能
2. 筛选面板标签样式优化
3. 标题栏功能增强
4. 整体页面吸引用户的新功能

---

## 二、功能规划

### 🔍 模块 A：搜索增强

#### A1. 搜索实时高亮
- **功能描述**：搜索时，匹配的关键字在卡片文件名中高亮显示（红色/主题色标记）
- **实现要点**：
  - 在 `WallpaperCard.vue` 中添加高亮渲染逻辑
  - 创建 `highlightText` 工具函数，支持正则分割和高亮包裹
  - 高亮样式：背景色 + 文字加粗
- **涉及文件**：
  - `src/utils/format.js` - 新增高亮函数
  - `src/components/wallpaper/WallpaperCard.vue` - 文件名高亮渲染
  - `src/composables/useFilter.js` - 导出搜索关键字供组件使用

#### A2. 搜索建议下拉
- **功能描述**：输入时实时显示匹配的文件名建议列表
- **实现要点**：
  - 在 `SearchBar.vue` 下方添加建议面板
  - 最多显示 5-8 条建议
  - 支持键盘上下选择、回车确认
  - 点击建议项直接填充搜索框
- **涉及文件**：
  - `src/components/common/SearchBar.vue`

#### A3. 搜索历史记录
- **功能描述**：记录用户最近搜索，快速选择
- **实现要点**：
  - localStorage 存储最近 5 条搜索记录
  - 点击搜索框时显示历史记录
  - 支持清除单条/全部历史

---

### 🎨 模块 B：筛选面板优化

#### B1. 标签文字加粗
- **功能描述**：「格式」「排序」两个标签文字加粗
- **实现要点**：
  - 修改 `.filter-label` 样式
  - `font-weight: 600` (semibold)
- **涉及文件**：
  - `src/components/common/FilterPanel.vue`

#### B2. 筛选面板视觉增强
- **功能描述**：提升筛选区域的视觉层次
- **实现要点**：
  - 添加微妙的背景色区分
  - 下拉框悬停/激活状态动画
  - 筛选数量标签动画效果

---

### 🏠 模块 C：标题栏功能增强

#### C1. 收藏夹功能
- **功能描述**：用户可收藏喜欢的壁纸
- **实现要点**：
  - 标题栏添加收藏夹图标按钮
  - 点击显示收藏的壁纸列表（侧边栏或弹窗）
  - localStorage 存储收藏数据
  - 卡片上添加收藏/取消收藏按钮
- **涉及文件**：
  - `src/composables/useFavorites.js` - 新建
  - `src/components/layout/AppHeader.vue`
  - `src/components/wallpaper/WallpaperCard.vue`
  - `src/components/common/FavoritesDrawer.vue` - 新建

#### C2. 视图切换
- **功能描述**：网格/列表/瀑布流三种视图模式
- **实现要点**：
  - 标题栏添加视图切换按钮组
  - 三种布局：网格(默认)、列表、瀑布流
  - localStorage 记住用户偏好
- **涉及文件**：
  - `src/composables/useViewMode.js` - 新建
  - `src/components/layout/AppHeader.vue`
  - `src/components/wallpaper/WallpaperGrid.vue`

#### C3. 全屏浏览模式
- **功能描述**：一键进入沉浸式全屏浏览
- **实现要点**：
  - 标题栏添加全屏按钮
  - 全屏时隐藏标题栏和筛选栏
  - ESC 或点击退出全屏

#### C4. 随机壁纸按钮
- **功能描述**：随机展示一张壁纸
- **实现要点**：
  - 标题栏添加骰子图标按钮
  - 点击随机选择一张壁纸并打开预览弹窗
  - 添加旋转动画效果

---

### ✨ 模块 D：整体页面吸引力提升

#### D1. 首页 Hero 动态背景
- **功能描述**：Hero 区域添加动态渐变背景
- **实现要点**：
  - CSS 渐变动画背景
  - 或使用随机壁纸作为模糊背景
  - 配合滚动渐变淡出效果

#### D2. 壁纸卡片悬停预览
- **功能描述**：鼠标悬停时显示放大预览
- **实现要点**：
  - 悬停 500ms 后显示更大的预览图
  - 预览图跟随鼠标位置
  - 显示更多信息（分辨率、大小等）

#### D3. 今日精选/每日壁纸
- **功能描述**：首页顶部展示今日精选壁纸
- **实现要点**：
  - 根据日期计算展示的壁纸
  - 大图展示 + 动画效果
  - 一键设置/下载

#### D4. 标签云/分类导航
- **功能描述**：根据文件名提取关键词形成标签云
- **实现要点**：
  - 自动从文件名提取关键词（如"原神"、"动漫"、"风景"等）
  - 点击标签快速筛选
  - 热门标签大小不同

#### D5. 回到顶部按钮
- **功能描述**：滚动后显示回到顶部按钮
- **实现要点**：
  - 滚动超过一屏后显示
  - 平滑滚动动画
  - 圆形按钮 + 箭头图标

#### D6. 无限滚动/懒加载优化
- **功能描述**：大量壁纸时分批加载
- **实现要点**：
  - 首次加载 20 张
  - 滚动到底部自动加载更多
  - 加载动画 + 骨架屏

#### D7. 键盘快捷键支持
- **功能描述**：键盘操作增强
- **实现要点**：
  - `/` 聚焦搜索框
  - `ESC` 关闭弹窗
  - `←` `→` 弹窗中切换壁纸
  - `F` 收藏当前壁纸
  - `R` 随机壁纸

#### D8. 壁纸统计信息
- **功能描述**：Footer 或 Hero 展示统计
- **实现要点**：
  - 总壁纸数量
  - 各格式数量
  - 总大小等

---

## 三、优先级排序

### 🔴 P0 - 必做（本次迭代）
1. **A1** 搜索实时高亮
2. **B1** 筛选标签加粗

### 🟠 P1 - 高优先级
3. **C4** 随机壁纸按钮
4. **D5** 回到顶部按钮
5. **D7** 键盘快捷键（搜索框 `/`）
6. **A2** 搜索建议下拉

### 🟡 P2 - 中优先级
7. **C1** 收藏夹功能
8. **D4** 标签云/分类导航
9. **D3** 今日精选
10. **C2** 视图切换

### 🟢 P3 - 低优先级（后续迭代）
11. **D1** Hero 动态背景
12. **D2** 悬停预览
13. **C3** 全屏浏览
14. **D6** 无限滚动
15. **A3** 搜索历史
16. **B2** 筛选面板视觉增强
17. **D8** 统计信息

---

## 四、P0 任务详细设计

### A1. 搜索实时高亮

#### 技术方案

```javascript
// src/utils/format.js - 新增
export function highlightText(text, keyword) {
  if (!keyword || !text) return [{ text, highlight: false }]

  const regex = new RegExp(`(${escapeRegExp(keyword)})`, 'gi')
  const parts = text.split(regex)

  return parts.map(part => ({
    text: part,
    highlight: regex.test(part)
  }))
}

function escapeRegExp(string) {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}
```

#### 组件改造

```vue
<!-- WallpaperCard.vue -->
<p class="card-filename">
  <template v-for="(part, index) in highlightedFilename" :key="index">
    <mark v-if="part.highlight" class="highlight">{{ part.text }}</mark>
    <span v-else>{{ part.text }}</span>
  </template>
</p>

<style>
.highlight {
  background: var(--color-accent-light);
  color: var(--color-accent);
  font-weight: 600;
  padding: 0 2px;
  border-radius: 2px;
}
</style>
```

#### 数据流
1. `useFilter` 导出 `searchQuery`
2. `App.vue` 传递 `searchQuery` 给 `WallpaperGrid`
3. `WallpaperGrid` 传递给每个 `WallpaperCard`
4. `WallpaperCard` 使用 `highlightText` 渲染文件名

---

### B1. 筛选标签加粗

#### 改动点

```scss
// FilterPanel.vue
.filter-label {
  font-size: $font-size-sm;
  font-weight: $font-weight-semibold; // 改为 semibold (600)
  color: var(--color-text-primary); // 改为主色
  white-space: nowrap;
}
```

---

## 五、验收标准

### A1 搜索实时高亮
- [ ] 输入搜索词后，匹配的文件名部分高亮显示
- [ ] 高亮支持中英文混合
- [ ] 高亮不区分大小写
- [ ] 清空搜索后高亮消失
- [ ] 高亮样式美观，与整体设计风格一致

### B1 筛选标签加粗
- [ ] 「格式」「排序」文字加粗显示
- [ ] 深色/浅色主题下都清晰可见

---

## 六、预估工作量

| 任务 | 预估 |
|------|------|
| A1 搜索实时高亮 | 中等 |
| B1 筛选标签加粗 | 简单 |
| 测试验证 | 简单 |

---

## 七、风险与注意事项

1. **性能风险**：高亮计算需考虑大量壁纸时的性能，使用 computed 缓存
2. **兼容性**：正则表达式需考虑特殊字符转义
3. **样式一致性**：高亮样式需适配深色/浅色主题

---

## 八、后续迭代建议

本次完成 P0 任务后，建议下一迭代优先实现：
1. 随机壁纸按钮 - 增加趣味性
2. 键盘快捷键 - 提升效率
3. 收藏夹功能 - 增加用户粘性
4. 标签云 - 提升内容发现效率
